# CLAUDE.md — data-collector 工作约定

个人自用工具，别过度设计。改动前先看 [`docs/sidepanel-states.md`](docs/sidepanel-states.md)（侧栏状态机与错误矩阵）和 [`docs/sinks.md`](docs/sinks.md)（去向与路由）。

## 一、每次迭代必须 bump 版本号

**只要改了代码并提交，就把补丁号 +1**（`0.2.1` → `0.2.2`）。

原因：用户在浏览器里**无法判断自己加载的是不是最新构建**。侧栏右下角显示
`v<版本> · <短 sha>`（打包时由 `scripts/build.mjs` 烙进产物），版本号不动的话
这个标记就失去意义，只能靠「某个功能怎么没出现」去反推——已经真实踩过一次。

要一起改的地方（`tests/unit/package.test.ts` 会校验三处一致）：

- `package.json`
- `packages/{shared,bridge,extension}/package.json`（含 `@data-collector/shared` 依赖版本）
- `packages/extension/manifest.json`
- `packages/shared/src/identity.ts` 的 `APP_VERSION` + `tests/unit/identity.test.ts`

改完跑 `npm install --package-lock-only` 同步 lockfile。

## 二、只在 `master` 上工作

不开功能分支、不走 PR。这是个人工具，Markdown 和小改动直接落主线。
平台默认开了别的分支就先 `git checkout master`。

## 二之二、产品链路是三段的，别把它压扁

```
采集 → 本机库（唯一落点 / 唯一去重依据，可增删改查）
     → 「已入库」里人工核对
     → 显式同步到 <repo>/_inbox（逐条或批量，commit + 尝试 push）
     → 用户自己在 Agent 里拉收件箱归档
```

**绝不在采集阶段就投递到收件箱**。那样用户失去中间那道核对，出问题也分不清是采错了
还是投错了；本机库也不再是唯一的去重依据。之前就是这么做的，实践证明不好用。

同步的三条硬约束：一条失败不影响其余；推送失败不算同步失败（条目已提交进仓库）；
重新采集同一地址 → 本地仍一条，但同步状态回到未同步。
端到端验证见 `tests/integration/pipeline.test.ts`（对着真 git 仓库验 commit）。

## 三、绝不能做的事（回归红线）

1. **绝不刷新用户的页面**。知识星球的分类（精华 / 最新）是应用内状态，不在 URL 里；
   刷新会退回「最新」，用户在精华页发起的采集就采成了别的内容。自愈一律用
   `chrome.scripting.executeScript` 注入，清单见 `packages/extension/src/background/injection.ts`
   ——**两个脚本都要补**（内容脚本 + 主世界帖子号钩子）。
2. **绝不改变页面外观**。处理过的帖子只打不可见属性标记，绝不 `display:none`——
   用户要能肉眼核对采到的内容对不对。
3. **绝不把浏览器的原生英文报错摆给用户看**，也绝不让它盖住写好的终态屏。
4. **绝不猜帖子地址**。对不上号就如实计入「已跳过」——猜错会让两条内容写到同一个文件上。
5. **绝不静默失败**。跳过、截断、打不开文件夹，都要在界面上说出来。
6. **绝不猜发布时间的年份，但站点写了年份就得认**。`new Date('03-05')` 不会失败，
   它返回 **2001-03-05**，还会以 `date_source: published`（「可信」）写进收件箱——
   只有月日的一律返回 undefined，退回采集时间并标 `collected`。
   反过来也栽过：一度要求「必须 4 位年份」，把老帖的 `23年06月18日` 也拒了，
   2023 年的帖子记成了采集当天。**两位数年份是站点写出来的，不是猜的**，三段就认。
   最可靠的来源是接口响应里的 `create_time`（完整时间戳）——既然已经靠它对上了帖子号，
   发布时间就该一并用上，别去解析页面上那行渲染出来的字。
7. **计数必须和明细同源**。「已跳过 N」和明细里的行数从同一个数组数出来，
   不能一个在循环外累加、一个在循环内 push——中途 break 就会对不上。
8. **帖子号索引必须由主世界钩子留存**。`TopicIndex` 是内容脚本的模块级变量，
   扩展一重载 / 自愈一注入就清零；而页面上的老帖子还在、它们的接口响应不会重来。
   钩子活在页面里不受影响，由它按帖子号留一份，内容脚本启动时整批要回来。
   留存集挂在 window 上跨实例共享，且只有当前生效的那份实例应答。
9. **收件箱目录名只认稳定内容 ID**。日期和标题都会变，按当次值重算目录名会让
   重采后同步长出第二份，Agent 会把同一篇归档两遍。
10. **绝不把服务这边的环境问题说成用户机器坏了**。本机服务是登录项（launchd / systemd），
    它的 PATH 只有 `/usr/bin:/bin:/usr/sbin:/sbin`，和终端里的**不是一回事**——
    用户终端里跑的是 `/opt/homebrew/bin/git`，服务里却只找得到 macOS 那层要 `xcrun` 的壳。
    真出过一次：据此断言「这台 Mac 的命令行工具坏了，去 `xcode-select --install`」，
    而用户的 git 一直好好的。**外部命令一律探到绝对路径再用**（`packages/bridge/src/git.ts`），
    报错要指向服务的环境、并列出试过哪些位置。

## 四、提交前

```bash
npm run typecheck && npm test && npm run package
```

改到批量采集或注入相关的代码时再加 `npm run test:e2e`（只有它能发现打包产物级别的问题，
比如内容脚本里混进 `export` 导致整个文件语法错误）。

## 五、选题过滤

用户明确不看的类别在采集端就跳过（目前内置**打新 / 楼市 / 相亲**），规则在
`packages/extension/src/topicFilter.ts`。被跳过的条目**照样出现在「本轮明细」里**，
状态是已跳过、原因写明类别——绝不静默丢弃。

判据三条：强信号 ≥ 1、不同信号 ≥ 2、**强信号必须出现在开头 150 字的主线窗口里**。
第三条是关键——排除的是以该话题为主线的帖子，不是提到该词的帖子。少了它，
三千字投资长文深处顺口一句「房价」「首付」就会被整篇扔掉（实测误伤：香港保险、
沪深300 薪酬统计、问减仓计划）。它会带来漏判（开头讲故事、中段才转楼市的留下来），
这是有意的：误判比漏判严重得多。

信号词本身也要防串台：`成交量`／`挂牌`（股市高频）、`年龄`／`身高`（养老险满篇）
都因此拿掉过。**新增规则时先写误伤用例再写规则**——「背着房贷加杠杆买股票」
主线是投资，必须留下。

跳过原因里要带上命中的词（`楼市内容（命中：房价、首付）`），否则误伤时无从下手。
