# 安全与隐私

## 数据边界

Data Collector 只读取用户主动打开或通过本机任务明确提交的受支持页面。内容在 Chrome 扩展、本机回环 Bridge 和本机知识库之间流动；0.1.0 不上传云端，不包含遥测。

扩展不会读取或保存 Cookie、密码、LocalStorage、请求头或浏览历史。知识星球访问完全复用用户浏览器已有会话；遇到登录、验证码或访问限制时要求用户处理，不尝试绕过。

## 防护

- Bridge 强制绑定 `127.0.0.1`，拒绝外网监听。
- 一次性 6 位码只用于换取随机 256 位令牌；有效期 10 分钟。
- 令牌文件权限为 `0600`，比较使用定时安全方法。
- WebSocket 同时验证 bearer token 和 Chrome 扩展 Origin。
- 只允许两个来源的 HTTPS URL；规范化时删除跟踪参数。
- HTML 按标签和属性白名单清洗，脚本、事件处理器和危险 URL 不写入知识库。
- 输出路径经过根目录约束和安全 slug 处理，阻止路径穿越。
- 图片只允许公开 HTTPS 目标；每次重定向和 DNS 结果都会拒绝回环、局域网、链路本地与保留地址。响应按流读取，限制数量、10 MB 硬上限、时间与安全位图 MIME 类型；SVG 不落盘。
- 回传文档的规范 URL、来源和内容类型必须与任务一致；协议违规的 WebSocket 会以 `1008` 关闭。
- 所有任务和目录写操作在进程内串行化，避免并发覆盖；知识库写入拒绝符号链接逃逸。
- 文件和目录索引采用原子写入。
- 发布打包使用固定文件白名单并扫描常见凭证模式。

## 本机敏感文件

- `~/.data-collector/auth.json`：本机 Bridge 令牌。不要提交到 Git 或分享。
- `~/Documents/data-collector`：可能包含付费或私有内容，应按来源协议和授权范围使用并备份。

如怀疑令牌泄露，停止 Bridge，删除 `~/.data-collector/auth.json`，重新启动并配对。扩展中可清除站点数据或重新加载扩展以移除旧令牌。

## 报告问题

安全报告应包含版本、操作系统、最小复现步骤和已脱敏日志。不要附带知识星球 Cookie、bearer token、私有正文或未脱敏页面快照。
