# 安全与隐私

## 数据边界

Data Collector 0.2.0 只读取用户主动打开或通过本机任务明确提交的受支持页面。内容在 Edge 扩展、本机回环 Bridge 和本机知识库之间流动；默认不上传云端，不包含遥测。

扩展不会读取或保存 Cookie、密码、LocalStorage、请求头或浏览历史。知识星球访问复用浏览器已有会话；遇到登录、验证码或访问限制时要求用户处理，不尝试绕过。

## 自动授权与固定身份

发布 manifest 包含固定公钥，因此每次从正式构建安装都会得到同一个扩展 ID。全新扩展先从 Bridge 健康响应确认受信任 ID，再以自身 `chrome.runtime.id` 和 WebSocket Origin 请求自动授权。Bridge 只向完全匹配的固定 Origin 返回随机 256 位 bearer token；令牌保存于扩展本地存储和权限为 `0600` 的本机配置文件。

这项取舍消除了人工输入和一次性凭据，但把信任锚点改为“固定发布身份 + 浏览器提供的 Origin + 回环本机进程”：

- 普通网页无法提供扩展 Origin，其他扩展的 ID 也不匹配，因此会被拒绝。
- 固定公钥与扩展 ID 是公开标识，不是秘密凭据。
- 已经以同一用户身份控制本机的恶意软件可以读取用户文件、操纵浏览器或伪造本机网络请求；这类同用户本机入侵不在本产品安全边界内。

## 其他防护

- Bridge 强制绑定 `127.0.0.1`，拒绝外网监听。
- bearer token 至少 256 位，文件权限为 `0600`，比较使用定时安全方法。
- WebSocket 同时验证 token 与固定扩展 Origin；首次自动授权也必须通过 Origin 和固定 ID 检查。
- 只允许两个来源的 HTTPS URL；规范化时删除跟踪参数。
- HTML 按标签和属性白名单清洗，脚本、事件处理器和危险 URL 不写入知识库。
- 输出路径经过根目录约束和安全 slug 处理，阻止路径穿越。
- 图片只允许公开 HTTPS 目标；每次重定向和 DNS 结果都会拒绝回环、局域网、链路本地与保留地址。响应按流读取，限制数量、10 MB 硬上限、时间与安全位图 MIME 类型；SVG 不落盘。
- 回传文档的规范 URL、来源和内容类型必须与任务一致；协议违规的 WebSocket 以 `1008` 关闭。
- 所有任务和目录写操作在进程内串行化；知识库写入拒绝符号链接逃逸并采用原子替换。
- 发布打包只允许六个 manifest 所需文件，扫描常见凭证模式，并生成可复现 ZIP。

## 本机敏感文件与恢复

- `~/.data-collector/auth.json`：本机 Bridge 令牌。不要提交到 Git 或分享。
- `~/Documents/data-collector`：可能包含付费或私有内容，应按来源协议和授权范围使用并备份。

如怀疑令牌泄露，停止 Bridge，删除 `~/.data-collector/auth.json`，在 `edge://extensions` 删除 Data Collector，重新启动 Bridge，并从最新 `artifacts/data-collector-extension` 安装。新扩展会自动取得新令牌。

## 报告问题

安全报告应包含版本、操作系统、最小复现步骤和已脱敏日志。不要附带知识星球 Cookie、bearer token、私有正文或未脱敏页面快照。
